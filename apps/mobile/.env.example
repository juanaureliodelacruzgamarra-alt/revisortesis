# The API URL the mobile client hits.
# Android emulator: 10.0.2.2 maps to the host machine.
# Real device on same Wi-Fi: use the host LAN IP (e.g. 192.168.1.50:8005).
# iOS simulator: localhost works.
EXPO_PUBLIC_API_URL=http://10.0.2.2:8005
